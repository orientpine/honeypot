---
name: {{ name }}-common
description: |
  {{ name | title }} 연구 그룹의 논문 작성을 위한 공통 스타일 가이드.
  측정값 포맷, 공급업체 인용, 어휘 패턴 등 전체 섹션에 적용되는 작성 규칙을 제공합니다.
  챕터별 스킬에서 참조됩니다.
---

# {{ name | title }} Research Group - Common Style Guide

이 스킬은 {{ name | title }} 연구 그룹의 논문 작성에 사용되는 기본 규칙을 제공합니다.

## Core Principles

{% for principle in principles %}
{{ loop.index }}. **{{ principle.name }}**: {{ principle.description }}
{% endfor %}

## Measurement Formatting

| Type | Format | Examples |
|------|--------|----------|
{% for mtype, mformat in measurement_format.items() %}
| {{ mtype | title }} | `{{ mformat.pattern }}` | {{ mformat.examples | join(', ') }} |
{% endfor %}

## Citation Style

- **Format**: {{ citation_style.format }}
- **Pattern**: `{{ citation_style.pattern }}`
- **Examples**: {{ citation_style.examples | join(', ') }}

## Vocabulary Patterns

### High Frequency Verbs by Section

{% for section, verbs in high_freq_verbs.items() %}
#### {{ section | title }}
{% for verb, count in verbs[:5] %}
- {{ verb }} ({{ count }})
{% endfor %}

{% endfor %}

### Transition Phrases

{% for section, phrases in transition_phrases.items() %}
#### {{ section | title }}
{% for phrase in phrases[:3] %}
- "{{ phrase.phrase }}"
{% endfor %}

{% endfor %}

### Hedging Expressions

| Expression | Usage Context |
|------------|---------------|
{% for expr in hedging_expressions[:8] %}
| {{ expr }} | Discussion, interpretation |
{% endfor %}

## Field Characteristics

- **Primary Field**: {{ field_characteristics.primary_field }}
- **Secondary Fields**: {{ field_characteristics.secondary_fields | join(', ') }}
- **Research Type**: {{ field_characteristics.research_type }}

### Key Terms
{% for term in field_characteristics.keywords[:10] %}
- {{ term }}
{% endfor %}

## Output Directory

**IMPORTANT**: All generated documents must be saved in `output/{paper_topic}/` folder.

- **Input source**: `resource/{paper_topic}/`
- **Output path**: `{project_root}/output/{paper_topic}/`

---

*Auto-generated by Paper Style Generator*
*Analysis confidence: {{ confidence }}%*
*Source papers: {{ paper_count }}*
